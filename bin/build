#!/bin/sh

# exit on error
set -e

buildPackage () {
	(
		package=$1
		echo "building package: $package"
		cd "$package"
		npm run build --if-present
	)
}

for package in ./packages/*
do
	buildPackage $package
done
