#!/bin/sh

# exit on error
set -e

for package in ./packages/*
do
	(
		echo "testing package: $package"
		cd "$package"
		THIS_SUBPACKAGE_PATH="$(pwd)/../../node_modules/.bin:$PATH"
		PATH=$THIS_SUBPACKAGE_PATH npm run test --if-present
	)
done
