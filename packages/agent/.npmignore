# We work with a safelist here, so block everything that's not permitted, and add packages
# that are.
**

!package.json
!README.md
!LICENSE

!src/**/*.ts
src/**/*.test.ts
src/**/__certificates__
src/package.json

!lib/**
lib/**/*.test.*
lib/**/*.tsbuildinfo
 