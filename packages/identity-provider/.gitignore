build_info.json
node_modules/
dist/
**/*.js
**/*.js.map
**/*.d.ts

# Cannot ignore .d.ts files in types/
!types/**/*.d.ts

# Cannot ignore setup files for webpack and jest, which are still JavaScript.
!webpack.config.js
!webpack.config.dev.js
!jest.config.js
!test-setup.js

# Used for smoke testing during development, a pain to git commit
__snapshots__